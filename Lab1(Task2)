# Функція для підрахунку кількості ударів годинника за певну годину
def clock_strikes(hour):
    # Якщо 00 годин, годинник не б'є
    if hour == 0:
        return 0
    # Годинник б'є стільки разів, скільки зараз годин
    else:
        return hour


# Основна функція для підрахунку кількості ударів від 00 до 15 год.
def main():
    total_strikes = 0  # Змінна для збереження загальної кількості ударів

    # Цикл для кожної години від 0 до 15
    for hour in range(16):
        strikes = clock_strikes(hour)  # Підрахунок ударів для поточної години
        total_strikes += strikes  # Додавання ударів до загального рахунку
        print(f"О {hour} годині годинник б'є {strikes} раз(ів).")

    # Виведення загальної кількості ударів
    print(f"Загальна кількість ударів годинника від 00 до 15 год: {total_strikes}")


# Виклик основної функції
main()
